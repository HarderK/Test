












		this.x *= ratio;
		this.y *= ratio;
		total += shares * price;
		var oldvalue = Math.sqrt(this.x * this.x + this.y * this.y);
		var price = getquote(stock);
		var ratio = newvalue / oldvalue;
		var shares = portfolio[stock];
	F.prototype = o;
	for(var stock in portfolio){
	function F(){}
	get r() {return Math.sqrt(this.x * this.x + this.y * this.y);},
	get theta() {return Math.atan2(this.y, this.x);}
	if(Object.create) return Object.create(p);
	if(p == null) throw TypeEror();    //p不能是null
	if(t !== "array" && t !== "function") throw TypeError();  //p只能是对象或者函数
	return new F();
	return object(p);
	return total;
	set r(newvalue){
	var t = typeof p;
	var total = 0.0;
	x : 1.0,
	y : 1.0,
	}
	},
/*q.x = 2, q.y = 3;
//原型式继承 ECMAScript中存在Object.create()方法实现原型式继承
console.log(o);
console.log(p);
console.log(p.r);*/
console.log(q);
console.log(q);
console.log(q);
console.log(q.__proto__);
console.log(q.r);
console.log(q.x + q.y);
console.log(s);
function getValue(portfolio){
function inherit(p){
function object(o){
o.x = 1;
p.y = 10;
p.y = 2;
q.r = 3;
q.x = 2, q.y = 5; //不能通过对象实例重写原型中的值
q.z = 3;
var o = {};
var p = inherit(o); //p继承o和Object.prototype
var p = {
var q = inherit(p);
var q = inherit(p);
var s = q.toString();
}
}
}
}